# Лабораторная работа 1 — Метод градиентного спуска

## Задание

Нужно минимизировать функцию двух переменных

\[
f(x, y) = a x + b y + e^{c x^2 + d y^2}
\]

методом градиентного спуска с дроблением шага и точностью по критерию остановки  
\(\|\nabla f(x, y)\| < \varepsilon\), где \(\varepsilon = 10^{-4}\). :contentReference[oaicite:0]{index=0}  

Коэффициенты \(a, b, c, d\) берутся из таблицы по номеру варианта (должно быть два запуска: верхняя и нижняя строка для твоего варианта). Отдельно в отчёте нужно сравнить поведение алгоритма при разных коэффициентах (скорость сходимости, шаг, «форму» функции).

## Что сделано

Реализация на Python

**Функция и параметры**

- В коде зашита функция  
  `f(x, y) = A * x + B * y + exp(C * x^2 + D * y^2)`,  
  где `A, B, C, D` задаются под вариант 20:
  -`A = 20.0`, `B = 0.5`, `C = 3.81`, `D = 0.30`;
- Используются `numpy.exp` и `math.sqrt`.

**Градиент и итерации**

- Введён класс `Point(x, y)` для хранения точки.
- Градиент считается аналитически:
  - \(\frac{∂f}{∂x} = A + 2 C x e^{C x^2 + D y^2}\)
  - \(\frac{∂f}{∂y} = B + 2 D y e^{C x^2 + D y^2}\)
- Шаг градиентного спуска:
  ```python
  p_{k+1} = p_k - α_k * ∇f(p_k)
